
export default interface Product {
    productId: string;
    name: string;
    draftReleasesCount: number;
    publishedReleasesCount: number;
    description: string;
    model: string;
}
